🚀 Java Web CRUD Application (MVC Architecture)
A professional web application demonstrating a complete CRUD (Create, Read, Update, Delete) flow using Java EE technologies. This project follows the MVC (Model-View-Controller) design pattern to ensure clean code separation and scalability.

🛠 Tech Stack
Backend: Java Servlets (Jakarta EE)

Frontend: JSP (JavaServer Pages), JSTL, Expression Language (EL)

Database: MySQL

Driver: JDBC (Java Database Connectivity)

Design: CSS3, FontAwesome Icons, Responsive Layouts

✨ Key Features
User Registration: Secure data entry into MySQL database.

Real-time Data Display: Fetching and listing database records dynamically.

Secure Updates: Pre-filling forms with existing data for easy modification.

Delete Operation: Safe deletion of records with JavaScript confirmation alerts.

Form Validation: Client-side and server-side validation for data integrity.

🏗 Architecture Flow
Controller (Servlets): Handles GET/POST requests and manages data flow.

Model (DAO/POJO): Represents the data structure.

View (JSP): Provides a clean and interactive UI for the user.
